<?php
/**
 * Configuración general de la app (no credenciales de BD — esas van
 * en config/database.php, ver config/database.example.php).
 *
 * Cada quien copia este archivo como "config.php" (queda fuera de Git,
 * ver .gitignore) y ajusta BASE_URL si su carpeta local se llama distinto.
 */

define('BASE_URL', 'http://localhost/PDFlex/public');
